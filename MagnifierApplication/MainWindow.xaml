<Window 
    x:Class="MagnifierApplication.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:MagnifierApplication"
    mc:Ignorable="d"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    ShowInTaskbar="False"
    Width="200"
    Height="200"
    ResizeMode="NoResize"
    IsHitTestVisible="False">

    <!--
    This window is the visual magnifier lens. The window itself is transparent,
    the window inside is clipped into a circular shape, creating the magnifying lens effect.
    -->
    
    <Grid Margin="12">
        <Image Name="MagnifierImage" Stretch="Fill"/>

        <Ellipse Name="CircleBorder"
                 Stroke="Black"
                 StrokeThickness="3"
                 Fill="Transparent"
                 IsHitTestVisible="False">
            <Ellipse.Effect>
                <DropShadowEffect BlurRadius="12"
                                  ShadowDepth="8"
                                  Opacity="0.8"/>
            </Ellipse.Effect>
        </Ellipse>

        <Border Name="SquareBorder"
                BorderBrush="Black"
                BorderThickness="3"
                Background="Transparent"
                Visibility="Collapsed"
                IsHitTestVisible="False">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10"
                                  ShadowDepth="2"
                                  Opacity="0.5"/>
            </Border.Effect>
        </Border>
    </Grid>
</Window>
